{
  "buildCommand": "cd backend && npm install --legacy-peer-deps && npm run build && cd ../frontend && npm install --legacy-peer-deps && npm run build && cd .. && cp -r frontend/.vercel/output .vercel/",
  "installCommand": "echo 'skip'"
}
